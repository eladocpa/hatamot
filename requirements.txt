# ספריות שהכלי צריך כדי לעבוד
# מתקינים את כולן בפקודה אחת:  pip install -r requirements.txt

anthropic>=0.49.0        # החיבור ל-Claude (ה"עיניים" שקוראות את השיק)
playwright>=1.45.0       # שליטה בדפדפן (ה"ידיים" שלוחצות באתר)
openpyxl>=3.1.0          # קריאה וכתיבה של קבצי אקסל
thefuzz>=0.22.1          # התאמת שמות (ה"שדכן" שמתאים שם שיק ללקוח)
python-Levenshtein>=0.25.0  # מאיץ את ההתאמה (אופציונלי אבל מומלץ)
python-dotenv>=1.0.0     # קריאת מפתח ה-API מקובץ סביבה במקום מהקוד
requests>=2.31.0         # שליחת בקשות ל-API של Maven (הוצאת קבלות)
